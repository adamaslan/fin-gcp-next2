# =============================================================================
# Options MCP Backend - Environment Variables
# =============================================================================
# Copy this to .env and fill in values. NEVER commit .env files.

# Server Port (Cloud Run sets this automatically)
PORT=8080

# GCP Project ID
GCP_PROJECT_ID=ttb-lang1

# Gemini AI (optional - for AI-enhanced analysis)
# GEMINI_API_KEY=your-gemini-api-key-here

# Frontend URL for CORS (set in production)
# FRONTEND_URL=https://your-nextjs-app.vercel.app

# Finnhub API (required for options pipeline)
FINHUB_API_KEY=your-finnhub-api-key

# Alpha Vantage API (optional - fallback for historical candle data)
ALPHA_VANTAGE_KEY=your-alpha-vantage-key
